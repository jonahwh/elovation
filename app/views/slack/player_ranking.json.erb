{
  "response_type": "in_channel",
  <% if @ratings.present? %>
    "text": "<%= @player.name %> has a ranking of <%= @ratings.value %>"
  <% else %>
    "text": "<%= @player.name %> is not yet ranked. Challenge them!"
  <% end %>
}
